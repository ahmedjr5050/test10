import 'package:flutter/material.dart';

class AA extends StatelessWidget {
  const AA({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Center(
            child: Text("احمد صبرى"),
          ),
          Center(
            child: Text("AA"),
          ),
          Center(
            child: Text("AA"),
          ),
          Center(
            child: Text("AA"),
          ),
        ],
      ),
    );
  }
}
